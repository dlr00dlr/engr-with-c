#include <stdio.h>
#ifdef __unix__
# include <unistd.h>
#elif defined _WIN32
# include <windows.h>
#define sleep(x) 
#endif

int main(void)
{

n=

for(j=0;j<n;j++){for(i=0;i<n;i++)printf("*");}
printf("\n");

	return 0;	
}